export enum QueueNamesEnum {
    QUEUE_EMPLOYEE_NOTIFY = 'notify:employee',

    JOB_EMPLOYEE_CREATED = 'employee:created',
    JOB_EMPLOYEE_UPDATED = 'employee:updated',
    JOB_EMPLOYEE_DELETED = 'employee:deleted',
}
